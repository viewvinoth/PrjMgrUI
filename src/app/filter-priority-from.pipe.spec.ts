import { FilterPriorityFromPipe } from './filter-priority-from.pipe';

describe('FilterPriorityFromPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPriorityFromPipe();
    expect(pipe).toBeTruthy();
  });
});
