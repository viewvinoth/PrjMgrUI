import { FilterEndDatePipe } from './filter-end-date.pipe';

describe('FilterEndDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEndDatePipe();
    expect(pipe).toBeTruthy();
  });
});
