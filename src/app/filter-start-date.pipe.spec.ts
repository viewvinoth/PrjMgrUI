import { FilterStartDatePipe } from './filter-start-date.pipe';

describe('FilterStartDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStartDatePipe();
    expect(pipe).toBeTruthy();
  });
});
