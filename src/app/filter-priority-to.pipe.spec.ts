import { FilterPriorityToPipe } from './filter-priority-to.pipe';

describe('FilterPriorityToPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPriorityToPipe();
    expect(pipe).toBeTruthy();
  });
});
